---
phase: 04-form-polish-and-animations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - package.json
autonomous: true

must_haves:
  truths:
    - "Form uses brand purple (#7b42e7) for primary color throughout"
    - "motion package is installed and ready for use"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Brand color CSS variables"
      contains: "oklch(0.53 0.24 291)"
    - path: "package.json"
      provides: "motion dependency"
      contains: '"motion"'
  key_links: []
---

<objective>
Install the motion animation library and update CSS variables to use Frontier Tower brand colors.

Purpose: Prepare dependencies and theming for animation implementation in next plan
Output: motion package installed, brand purple applied to --primary CSS variable
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-form-polish-and-animations/04-RESEARCH.md

Key research findings:
- Use `motion` npm package (v12.x) with imports from "motion/react"
- Brand purple #7b42e7 = oklch(0.53 0.24 291)
- Update both :root and .dark themes for consistency
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install motion package</name>
  <files>package.json</files>
  <action>
    Install the motion animation library:
    ```bash
    npm install motion
    ```
    This is the industry-standard React animation library (formerly Framer Motion).
    Import path will be "motion/react" (not "framer-motion").
  </action>
  <verify>
    Run `npm ls motion` - should show motion@12.x installed
    Check package.json contains "motion" in dependencies
  </verify>
  <done>motion package installed and visible in package.json dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Update brand colors in globals.css</name>
  <files>src/app/globals.css</files>
  <action>
    Update the CSS variables in globals.css to use Frontier Tower brand purple:

    In :root section, change:
    - `--primary: oklch(0.205 0 0);` to `--primary: oklch(0.53 0.24 291);`
    - `--primary-foreground: oklch(0.985 0 0);` to `--primary-foreground: oklch(1 0 0);`
    - `--ring: oklch(0.708 0 0);` to `--ring: oklch(0.53 0.24 291);`

    In .dark section, change:
    - `--primary: oklch(0.922 0 0);` to `--primary: oklch(0.53 0.24 291);`
    - `--primary-foreground: oklch(0.205 0 0);` to `--primary-foreground: oklch(1 0 0);`
    - `--ring: oklch(0.556 0 0);` to `--ring: oklch(0.53 0.24 291);`

    The oklch(0.53 0.24 291) value is #7b42e7 (brand purple) converted to OKLCH color space.
    L=0.53 (53% lightness), C=0.24 (high chroma for vibrant purple), H=291 (purple hue angle).
  </action>
  <verify>
    Run `npm run dev` and visit http://localhost:3000/apply
    Verify buttons and focus rings now appear in purple (#7b42e7) instead of dark gray
    Check the loading spinner shows purple color
    Check progress indicator uses purple for completed steps
  </verify>
  <done>
    All primary color elements (buttons, focus rings, progress indicator, loading spinner) display brand purple
  </done>
</task>

</tasks>

<verification>
1. `npm ls motion` shows motion package installed
2. `grep "oklch(0.53 0.24 291)" src/app/globals.css` returns matches for --primary and --ring
3. Visual check: form buttons are purple, not gray
</verification>

<success_criteria>
- motion package installed (verified via npm ls)
- Brand purple (#7b42e7 / oklch(0.53 0.24 291)) applied to --primary CSS variable
- All primary-colored UI elements (buttons, progress indicator, focus rings) display brand purple
</success_criteria>

<output>
After completion, create `.planning/phases/04-form-polish-and-animations/04-01-SUMMARY.md`
</output>
