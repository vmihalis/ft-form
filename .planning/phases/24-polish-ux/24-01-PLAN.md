---
phase: 24-polish-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
  - src/components/ui/textarea.tsx
autonomous: true

must_haves:
  truths:
    - "Button hover/focus transitions smoothly over 200ms"
    - "Input focus ring animates smoothly over 200ms"
    - "Textarea focus ring animates smoothly over 200ms"
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "Button with duration-200 transition"
      contains: "duration-200"
    - path: "src/components/ui/input.tsx"
      provides: "Input with duration-200 transition"
      contains: "duration-200"
    - path: "src/components/ui/textarea.tsx"
      provides: "Textarea with duration-200 transition"
      contains: "duration-200"
  key_links: []
---

<objective>
Add 200ms transition duration to Button, Input, and Textarea components for smooth microinteractions.

Purpose: DS-06 requires 200-500ms transitions on all interactive elements. The base UI components need explicit duration-200 to ensure consistent, premium-feeling hover and focus state transitions.

Output: Button, Input, and Textarea components with smooth 200ms transitions on all state changes.
</objective>

<execution_context>
@/home/memehalis/.claude/get-shit-done/workflows/execute-plan.md
@/home/memehalis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-polish-ux/24-RESEARCH.md

@src/components/ui/button.tsx
@src/components/ui/input.tsx
@src/components/ui/textarea.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add duration-200 to Button transition</name>
  <files>src/components/ui/button.tsx</files>
  <action>
    Update the buttonVariants base class to include explicit `duration-200` after `transition-all`.

    Current: `transition-all disabled:pointer-events-none...`
    Change to: `transition-all duration-200 disabled:pointer-events-none...`

    The button already has `transition-all` but without explicit duration, it uses browser default. Adding `duration-200` ensures 200ms transitions on hover, focus, and active states for premium feel.
  </action>
  <verify>Check the updated button.tsx contains "duration-200" in the base class string</verify>
  <done>Button base class includes `duration-200` for consistent 200ms transitions</done>
</task>

<task type="auto">
  <name>Task 2: Add duration-200 to Input transition</name>
  <files>src/components/ui/input.tsx</files>
  <action>
    Update the Input className to include explicit `duration-200` for focus transitions.

    Current: `transition-[color,box-shadow] outline-none`
    Change to: `transition-[color,box-shadow] duration-200 outline-none`

    Also update transition property to include border-color for smooth focus ring:
    Change `transition-[color,box-shadow]` to `transition-all duration-200`

    This ensures the focus ring and border transitions smoothly over 200ms.
  </action>
  <verify>Check the updated input.tsx contains "duration-200" in the className</verify>
  <done>Input className includes `duration-200` for smooth focus transitions</done>
</task>

<task type="auto">
  <name>Task 3: Add duration-200 to Textarea transition</name>
  <files>src/components/ui/textarea.tsx</files>
  <action>
    First, read the current textarea.tsx to check its transition setup.

    Update the Textarea className to include:
    - `transition-all duration-200` for smooth state changes

    This ensures textarea focus ring and border transitions smoothly, matching Input behavior.
  </action>
  <verify>Check the updated textarea.tsx contains "duration-200" in the className</verify>
  <done>Textarea className includes `duration-200` for smooth focus transitions</done>
</task>

</tasks>

<verification>
1. All three files contain `duration-200` in their base classNames
2. Run `npm run build` to verify no TypeScript errors
3. Visual test: Focus on input/button should show smooth 200ms transition (not instant)
</verification>

<success_criteria>
- Button, Input, and Textarea components all have explicit 200ms transition duration
- No build errors
- DS-06 partially satisfied (interactive elements have microinteractions)
</success_criteria>

<output>
After completion, create `.planning/phases/24-polish-ux/24-01-SUMMARY.md`
</output>
