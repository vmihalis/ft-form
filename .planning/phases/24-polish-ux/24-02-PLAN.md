---
phase: 24-polish-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/dropdown-menu.tsx
  - src/components/ui/tooltip.tsx
  - src/components/ui/popover.tsx
  - src/components/ui/select.tsx
  - src/components/ui/sheet.tsx
  - src/components/ui/alert-dialog.tsx
autonomous: true

must_haves:
  truths:
    - "Dropdown menus have glass backdrop-blur effect"
    - "Tooltips have glass styling with semi-transparent background"
    - "Popovers have glass styling"
    - "Select dropdowns have glass styling"
    - "Sheets have glass backdrop-blur effect"
    - "Alert dialogs have glass-card styling for focused attention"
  artifacts:
    - path: "src/components/ui/dropdown-menu.tsx"
      provides: "DropdownMenuContent with glass styling"
      contains: "glass"
    - path: "src/components/ui/tooltip.tsx"
      provides: "TooltipContent with glass styling"
      contains: "glass"
    - path: "src/components/ui/popover.tsx"
      provides: "PopoverContent with glass styling"
      contains: "glass"
    - path: "src/components/ui/select.tsx"
      provides: "SelectContent with glass styling"
      contains: "glass"
    - path: "src/components/ui/sheet.tsx"
      provides: "SheetContent with glass styling"
      contains: "glass"
    - path: "src/components/ui/alert-dialog.tsx"
      provides: "AlertDialogContent with glass-card styling"
      contains: "glass-card"
  key_links: []
---

<objective>
Apply glassmorphism styling to all floating/portal UI components for premium visual consistency.

Purpose: DS-05 requires glassmorphism effects on modals, dropdowns, tooltips. This creates visual depth and aligns with the FrontierOS premium aesthetic established in Phase 20.

Output: All floating elements (DropdownMenuContent, TooltipContent, PopoverContent, SelectContent, SheetContent, AlertDialogContent) use glass/glass-card utilities from globals.css.
</objective>

<execution_context>
@/home/memehalis/.claude/get-shit-done/workflows/execute-plan.md
@/home/memehalis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-polish-ux/24-RESEARCH.md

@src/app/globals.css
@src/components/ui/dropdown-menu.tsx
@src/components/ui/tooltip.tsx
@src/components/ui/popover.tsx
@src/components/ui/select.tsx
@src/components/ui/sheet.tsx
@src/components/ui/alert-dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply glass styling to dropdown and tooltip components</name>
  <files>src/components/ui/dropdown-menu.tsx, src/components/ui/tooltip.tsx</files>
  <action>
    **DropdownMenuContent:**
    Replace `bg-popover` with `glass` in DropdownMenuContent className.

    Current: `"bg-popover text-popover-foreground data-[state=open]:animate-in...`
    Change to: `"glass text-popover-foreground data-[state=open]:animate-in...`

    Also update DropdownMenuSubContent the same way (bg-popover -> glass).

    **TooltipContent:**
    Replace `bg-foreground text-background` with `glass text-foreground` for semi-transparent tooltip.

    Current: `"bg-foreground text-background animate-in...`
    Change to: `"glass text-foreground animate-in...`

    Also update the TooltipPrimitive.Arrow className from `bg-foreground fill-foreground` to `fill-[var(--glass-bg)]` for arrow to match glass background.
  </action>
  <verify>Check dropdown-menu.tsx and tooltip.tsx contain "glass" class instead of solid bg</verify>
  <done>DropdownMenuContent, DropdownMenuSubContent, and TooltipContent use glass styling</done>
</task>

<task type="auto">
  <name>Task 2: Apply glass styling to popover and select components</name>
  <files>src/components/ui/popover.tsx, src/components/ui/select.tsx</files>
  <action>
    **PopoverContent:**
    Replace `bg-popover` with `glass` in PopoverContent className.

    Current: `"bg-popover text-popover-foreground data-[state=open]:animate-in...`
    Change to: `"glass text-popover-foreground data-[state=open]:animate-in...`

    **SelectContent:**
    Replace `bg-popover` with `glass` in SelectContent className.

    Current: `"bg-popover text-popover-foreground data-[state=open]:animate-in...`
    Change to: `"glass text-popover-foreground data-[state=open]:animate-in...`
  </action>
  <verify>Check popover.tsx and select.tsx contain "glass" class instead of bg-popover</verify>
  <done>PopoverContent and SelectContent use glass styling</done>
</task>

<task type="auto">
  <name>Task 3: Apply glass styling to sheet and alert dialog components</name>
  <files>src/components/ui/sheet.tsx, src/components/ui/alert-dialog.tsx</files>
  <action>
    **SheetContent:**
    Replace `bg-background` with `glass` in SheetContent className.

    Current: `"bg-background data-[state=open]:animate-in...`
    Change to: `"glass data-[state=open]:animate-in...`

    **AlertDialogContent:**
    Replace `bg-background` with `glass-card` in AlertDialogContent className.
    Use glass-card (heavier) for modals since they need more contrast for focused attention.

    Current: `"bg-background data-[state=open]:animate-in...`
    Change to: `"glass-card data-[state=open]:animate-in...`
  </action>
  <verify>Check sheet.tsx contains "glass" and alert-dialog.tsx contains "glass-card"</verify>
  <done>SheetContent uses glass and AlertDialogContent uses glass-card styling</done>
</task>

</tasks>

<verification>
1. All six component files updated with glass/glass-card classes
2. Run `npm run build` to verify no TypeScript errors
3. Visual test: Open any dropdown/tooltip/popover to see glass backdrop-blur effect
</verification>

<success_criteria>
- All floating elements have glass/glass-card styling
- Backdrop-blur effect visible on floating elements
- No build errors
- DS-05 fully satisfied (glassmorphism on floating elements)
</success_criteria>

<output>
After completion, create `.planning/phases/24-polish-ux/24-02-SUMMARY.md`
</output>
