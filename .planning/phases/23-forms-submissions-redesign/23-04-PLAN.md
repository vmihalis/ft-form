---
phase: 23-forms-submissions-redesign
plan: 04
type: execute
wave: 2
depends_on: ["23-03"]
files_modified:
  - src/components/admin/SubmissionsFilters.tsx
  - src/components/admin/submissions-columns.tsx
autonomous: true

must_haves:
  truths:
    - "Filters section has glass styling consistent with table"
    - "Table columns display status, form name, and date with clear visual hierarchy"
    - "Status badges, form names, and dates are readable at a glance"
  artifacts:
    - path: "src/components/admin/SubmissionsFilters.tsx"
      provides: "Glass-styled filters section"
      contains: "glass"
    - path: "src/components/admin/submissions-columns.tsx"
      provides: "Optimized column definitions"
  key_links:
    - from: "src/components/admin/SubmissionsTable.tsx"
      to: "SubmissionsFilters.tsx"
      via: "Renders filters above table"
      pattern: "SubmissionsFilters"
---

<objective>
Apply glass styling to the submissions filters and optimize information density in the table.

Purpose: Complete the submissions view polish with consistent glassmorphism styling and improved scanability (SUB-03).

Output: Glass-styled filters section and optimized table columns for better information density.
</objective>

<execution_context>
@/home/memehalis/.claude/get-shit-done/workflows/execute-plan.md
@/home/memehalis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-forms-submissions-redesign/23-RESEARCH.md

# Key files
@src/components/admin/SubmissionsFilters.tsx
@src/components/admin/submissions-columns.tsx
@src/components/admin/SubmissionsTable.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add glass styling to SubmissionsFilters</name>
  <files>src/components/admin/SubmissionsFilters.tsx</files>
  <action>
First read the current SubmissionsFilters.tsx to understand its structure, then apply glass styling to create visual consistency with the table.

Read the file first, then apply these updates:

1. Wrap the entire filters section in a glass container:
```typescript
<div className="glass-card rounded-2xl p-4">
  {/* existing filters content */}
</div>
```

2. Ensure filter inputs (Select, Input) have appropriate styling for glass backgrounds:
- The existing shadcn inputs should work, but may need slight adjustments
- Add `bg-background/50` to input elements if they appear invisible on glass

3. Ensure the SearchInput has appropriate styling:
```typescript
className="bg-background/50 border-glass-border"
```

4. Group related filters visually with proper spacing:
- Use `gap-4` for filter groups
- Ensure responsive layout works (stack on mobile, row on desktop)

5. The export button and count display should be clearly visible:
```typescript
<span className="text-sm text-muted-foreground whitespace-nowrap">
  {filteredCount} results
</span>
```

Example structure:
```typescript
export function SubmissionsFilters(props) {
  return (
    <div className="glass-card rounded-2xl p-4">
      <div className="flex flex-col sm:flex-row gap-4">
        {/* Filter controls row 1: Search + Form Filter */}
        <div className="flex flex-1 gap-4">
          {/* SearchInput */}
          {/* FormFilter */}
        </div>

        {/* Filter controls row 2: Status + Date Range */}
        <div className="flex flex-wrap gap-4">
          {/* StatusFilter */}
          {/* DateRangeFilter */}
        </div>

        {/* Results count + Export */}
        <div className="flex items-center gap-4 ml-auto">
          <span className="text-sm text-muted-foreground">
            {filteredCount} results
          </span>
          {/* ExportButton */}
        </div>
      </div>
    </div>
  );
}
```
  </action>
  <verify>
1. `npx tsc --noEmit` passes
2. Filters section displays with glass styling
3. All filter inputs are visible and functional
4. Layout responds properly on different screen sizes
  </verify>
  <done>SubmissionsFilters has glass-card container with properly styled filter inputs</done>
</task>

<task type="auto">
  <name>Task 2: Optimize submissions-columns for information density</name>
  <files>src/components/admin/submissions-columns.tsx</files>
  <action>
First read the current submissions-columns.tsx to understand the column definitions, then optimize for better information density.

Read the file first, then apply these updates:

1. Ensure the status column uses consistent badge styling that works on glass:
```typescript
{
  accessorKey: "status",
  header: "Status",
  cell: ({ row }) => {
    const status = row.getValue("status") as string;
    const config = statusConfig[status as keyof typeof statusConfig];
    return (
      <Badge variant="outline" className={config?.className}>
        {config?.label || status}
      </Badge>
    );
  },
  filterFn: (row, id, value) => {
    return value === undefined || row.getValue(id) === value;
  },
}
```

2. Update statusConfig with glass-friendly colors (similar to SubmissionSheet):
```typescript
const statusConfig = {
  new: {
    label: "New",
    className: "bg-blue-100/90 text-blue-800 border-blue-200 dark:bg-blue-900/50 dark:text-blue-400 dark:border-blue-800"
  },
  under_review: {
    label: "Under Review",
    className: "bg-yellow-100/90 text-yellow-800 border-yellow-200 dark:bg-yellow-900/50 dark:text-yellow-400 dark:border-yellow-800",
  },
  accepted: {
    label: "Accepted",
    className: "bg-green-100/90 text-green-800 border-green-200 dark:bg-green-900/50 dark:text-green-400 dark:border-green-800",
  },
  rejected: {
    label: "Rejected",
    className: "bg-red-100/90 text-red-800 border-red-200 dark:bg-red-900/50 dark:text-red-400 dark:border-red-800",
  },
};
```

3. Ensure date formatting is concise:
```typescript
{
  accessorKey: "submittedAt",
  header: "Submitted",
  cell: ({ row }) => {
    const date = new Date(row.getValue("submittedAt") as number);
    return (
      <span className="text-muted-foreground text-sm">
        {date.toLocaleDateString("en-US", {
          month: "short",
          day: "numeric",
          hour: "numeric",
          minute: "2-digit",
        })}
      </span>
    );
  },
  // ... filter function
}
```

4. If there's a form name column, ensure it's truncated properly:
```typescript
{
  accessorKey: "formName",
  header: "Form",
  cell: ({ row }) => (
    <span className="text-foreground font-medium truncate max-w-[200px] block">
      {row.getValue("formName")}
    </span>
  ),
  // ... filter function
}
```

5. Consider adding a sortable header indicator if not present (optional).
  </action>
  <verify>
1. `npx tsc --noEmit` passes
2. Status badges are clearly visible on glass table
3. Date and form name display concisely
4. Table is scannable with clear visual hierarchy
  </verify>
  <done>Table columns display with optimized styling for glass backgrounds and improved density</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no type errors
2. Filters section has glass-card container
3. Filter inputs are visible and functional on glass background
4. Status badges readable in both light and dark modes
5. Table information is scannable with clear hierarchy
6. Export button and count display work correctly
</verification>

<success_criteria>
- Filters section styled consistently with glass table (SUB-01 complete)
- Information density improved for scanning (SUB-03)
- Status badges clearly visible on glass (SUB-02 complete)
- All functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/23-forms-submissions-redesign/23-04-SUMMARY.md`
</output>
