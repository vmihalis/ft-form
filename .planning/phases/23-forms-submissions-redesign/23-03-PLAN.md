---
phase: 23-forms-submissions-redesign
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/admin/SubmissionsTable.tsx
  - src/components/admin/SubmissionSheet.tsx
autonomous: true

must_haves:
  truths:
    - "Submissions table displays with glassmorphism container styling"
    - "Submission detail panel has glass panel styling"
    - "Table rows are clearly readable against glass background"
  artifacts:
    - path: "src/components/admin/SubmissionsTable.tsx"
      provides: "Submissions table with glass container"
      contains: "glass-card"
    - path: "src/components/admin/SubmissionSheet.tsx"
      provides: "Detail panel with glass styling"
      contains: "glass"
  key_links:
    - from: "src/components/admin/SubmissionsTable.tsx"
      to: "SubmissionSheet.tsx"
      via: "Opens sheet on row click"
      pattern: "onRowClick"
---

<objective>
Apply glassmorphism styling to the submissions table container and detail panel.

Purpose: Complete the submissions management visual overhaul with glass styling consistent with the dashboard (SUB-01, SUB-02).

Output: Glass-styled submissions table wrapper and updated SubmissionSheet with glass panel styling.
</objective>

<execution_context>
@/home/memehalis/.claude/get-shit-done/workflows/execute-plan.md
@/home/memehalis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-forms-submissions-redesign/23-RESEARCH.md

# Key files
@src/components/admin/SubmissionsTable.tsx
@src/components/admin/SubmissionSheet.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add glass container to SubmissionsTable</name>
  <files>src/components/admin/SubmissionsTable.tsx</files>
  <action>
Wrap the submissions table in a glassmorphism container. Apply glass-card to the outer container, not individual rows.

Update `src/components/admin/SubmissionsTable.tsx`:

1. Import motion for smooth entrance animation:
```typescript
import { motion } from "motion/react";
```

2. Wrap the table section (the `<div className="rounded-md border">` wrapper) with glass-card styling:

Replace:
```typescript
<div className="rounded-md border">
  <Table>
```

With:
```typescript
<motion.div
  className="glass-card rounded-2xl overflow-hidden"
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.3 }}
>
  <Table>
```

And update the closing tag accordingly.

3. Update table row hover state for glass background - in the TableRow that maps submissions, update the className:

From:
```typescript
className="cursor-pointer hover:bg-muted/50"
```

To:
```typescript
className="cursor-pointer hover:bg-white/5 dark:hover:bg-white/5 transition-colors"
```

4. Update the skeleton to use glass styling too - wrap in glass-card:
```typescript
function TableSkeleton() {
  return (
    <div className="glass-card rounded-2xl overflow-hidden">
      <Table>
        {/* ... existing skeleton content ... */}
      </Table>
    </div>
  );
}
```

5. Ensure the empty state row has appropriate styling for glass background.
  </action>
  <verify>
1. `npx tsc --noEmit` passes
2. Run dev server and navigate to /admin, click Submissions tab
3. Table has glass styling with blur effect
4. Rows are readable and hover state works
  </verify>
  <done>Submissions table wrapped in glass-card container with smooth entrance animation</done>
</task>

<task type="auto">
  <name>Task 2: Apply glass styling to SubmissionSheet</name>
  <files>src/components/admin/SubmissionSheet.tsx</files>
  <action>
Add glass background styling to the SubmissionSheet detail panel.

Update `src/components/admin/SubmissionSheet.tsx`:

1. Update the SheetContent className to add glass styling. In all three places where SheetContent is rendered (empty state, loading state, main content), update:

From:
```typescript
<SheetContent className="w-full sm:w-[540px] sm:max-w-[calc(100vw-2rem)] overflow-y-auto">
```

To:
```typescript
<SheetContent className="glass w-full sm:w-[540px] sm:max-w-[calc(100vw-2rem)] overflow-y-auto">
```

Note: Use `glass` (lighter) instead of `glass-card` (heavier) for the panel since it's a side panel that overlays content, not a card.

2. Update section titles for better readability on glass:
The Section component should ensure text is readable. Update if needed:
```typescript
function Section({
  title,
  children,
}: {
  title: string;
  children: React.ReactNode;
}) {
  return (
    <div className="space-y-3">
      <h3 className="font-medium text-foreground">{title}</h3>
      {children}
    </div>
  );
}
```

3. Ensure separator uses the appropriate border color for glass:
The Separator component should already use border-border which works on glass, but verify.

4. Update status badge styling to work well on glass background. The statusConfig colors should remain visible. Optionally add a slight backdrop:
```typescript
const statusConfig = {
  new: { label: "New", className: "bg-blue-100/90 text-blue-800 border-blue-200 dark:bg-blue-900/50 dark:text-blue-400 dark:border-blue-800" },
  under_review: {
    label: "Under Review",
    className: "bg-yellow-100/90 text-yellow-800 border-yellow-200 dark:bg-yellow-900/50 dark:text-yellow-400 dark:border-yellow-800",
  },
  accepted: {
    label: "Accepted",
    className: "bg-green-100/90 text-green-800 border-green-200 dark:bg-green-900/50 dark:text-green-400 dark:border-green-800",
  },
  rejected: {
    label: "Rejected",
    className: "bg-red-100/90 text-red-800 border-red-200 dark:bg-red-900/50 dark:text-red-400 dark:border-red-800",
  },
};
```
  </action>
  <verify>
1. `npx tsc --noEmit` passes
2. Run dev server, navigate to /admin, click on a submission
3. Sheet slides in with glass styling
4. Text is readable on glass background
5. Status badges are clearly visible
  </verify>
  <done>SubmissionSheet displays with glass panel styling and readable text</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no type errors
2. Submissions table has glass-card container styling
3. Table rows are readable with appropriate hover states
4. Submission detail sheet has glass background
5. All text and badges are clearly readable on glass backgrounds
6. Both light and dark modes display correctly
</verification>

<success_criteria>
- Submissions table styled with glassmorphism design (SUB-01)
- Submission detail panel uses glass panel styling (SUB-02)
- Text and badges remain readable on glass backgrounds
- Smooth entrance animation on table load
</success_criteria>

<output>
After completion, create `.planning/phases/23-forms-submissions-redesign/23-03-SUMMARY.md`
</output>
